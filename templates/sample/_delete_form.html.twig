<form method="post" action="{{ path('app_sample_delete', {'id': sample.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet échantillon ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sample.id) }}">
	<button class="btn">Supprimer</button>
</form>

<!-- Navbar -->
<nav
	class="navbar navbar-expand-lg navbar-light bg-body-tertiary">
	<!-- Container wrapper -->
	<div
		class="container">

		<!-- Collapsible wrapper -->
		<div
			class="collapse navbar-collapse" id="navbarButtonsExample">
			<!-- Left links -->
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link" href="#">Dashboard</a>
				</li>
			</ul>
			<!-- Left links -->

			<div class="d-flex align-items-center">


				{% if app.user %}
					<a data-mdb-ripple-init class="btn btn-dark px-3" href="{{ path('app_account')}}" role="button">
						<i class="fas fa-user"></i>
					</a>
				{% else %}
					<button data-mdb-ripple-init class="btn btn-dark me-3" role="button" href="{{ path('app_register')}}">
						S'inscrire
					</button>
					<a data-mdb-ripple-init class="btn btn-dark px-3" role="button" href="{{ path('app_login')}}">
						<i class="fas fa-arrow-right-to-bracket"></i>
					</a>
				{% endif %}

			</div>
		</div>
		<!-- Collapsible wrapper -->
	</div>
	<!-- Container wrapper -->
</nav>
<!-- Navbar -->

{% extends 'base.html.twig' %}

{% block title %}Test
{% endblock %}

{% block body %}
	<h1>Test</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ test.id }}</td>
			</tr>
			<tr>
				<th>Titre</th>
				<td>{{ test.title }}</td>
			</tr>
			<tr>
				<th>Scenario</th>
				<td>{{ test.scenario }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_test_index') }}">Retourner à la liste des tests</a>

	<a href="{{ path('app_test_edit', {'id': test.id}) }}">Editer</a>

	{{ include('test/_delete_form.html.twig') }}
{% endblock %}

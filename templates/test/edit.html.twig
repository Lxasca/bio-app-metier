{% extends 'base.html.twig' %}

{% block title %}Editer un test
{% endblock %}

{% block body %}
	<h4 class="text-center">Editer un test</h4>

	<div class="container-table">
		{{ include('test/_form.html.twig') }}
	</div>

	<a href="{{ path('app_test_index') }}">Retourner à la liste des tests</a>

	{{ include('test/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_result_delete', {'id': result.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce résultat ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ result.id) }}">
	<button class="btn btn-rounded btn-light shadow mt-3 ms-3">Supprimer</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Editer le resultat
{% endblock %}

{% block body %}
	<h4 class="text-center">Editer le resultat</h4>

	<div class="container-table">
		{{ include('result/_form.html.twig') }}
	</div>

	<a href="{{ path('app_result_index') }}">Retourner à la liste des résultats</a>

	{{ include('result/_delete_form.html.twig') }}
{% endblock %}
